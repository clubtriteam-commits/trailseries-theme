<?php
declare( strict_types=1 );
/**
 * TEMP STUB — verifying the theme can override EventON's single event
 * template at all before building the real tabbed page. Delete/replace
 * once confirmed either way.
 */
get_header();
echo '<div style="padding:4rem 2rem;font-size:2rem;">TSR_STUB_TEMPLATE_MARKER_OK</div>';
get_footer();
